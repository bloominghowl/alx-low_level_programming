#include "main"

/**
 * _islower - checks for lowercase character
 * @c: single letter input
 * Return: 1 if int a is lowercase, 0 if otherwise
 */
int _islower(int c)
{
	if (a >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
