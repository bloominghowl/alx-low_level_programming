# Variables
CC = gcc
SRC = main.c utility.c
OBJ = $(SRC:.c=.o)
NAME = school

# Rule: all
all: $(NAME)

# Rule to build the executable
$(NAME): $(OBJ)
	$(CC) -o $(NAME) $(OBJ)

# Rule to compile source files to object files
%.o: %.c
	$(CC) -c $< -o $@

# Clean rule
clean:
	rm -f $(OBJ) $(NAME)
